`ifndef __NETTYPE_HEADER__
    `define __NETTYPE_HEADER__

    /***** default net type: choose one of two. *****/
    `default_nettype none       //none (recommended)
//  `default_nettype wire       //wire (Verilog standard)

`endif
